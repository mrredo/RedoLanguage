package ast

import "RedoLanguagev2/types"

func ParseTokens(tokens []types.Token) types.AST {
	var ast = types.AST{}
	for _, v := range tokens {
		switch v.Type {

		}
	}
	switch {

	}
	return ast
}
